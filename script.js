"use strict"

const calculator = {
    previous: null,
    current: null,
    operator: null,

    /*
    delete() {
        //code
    },
    */

    /*
    compute() {
        let result;
        switch(this.operator) {
            case "+":
                result = this.previous + this.current;
                break;
            case "-":
                result = this.previous - this.current;
                break;
            case "/":
                result = this.previous / this.current;
                break;
            case "*":
                result = this.previous * this.current;
                break;
            default:
                alert("No operator was chosen, cannot calculate!");
        }
        return result;
    }
    */
}